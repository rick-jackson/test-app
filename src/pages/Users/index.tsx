import UsersList from "@component/users/UsersList";
import Layout from "@component/layout/Pages";

const UsersPage = () => {
  return (
    <Layout>
      <UsersList />
    </Layout>
  );
};

export default UsersPage;
